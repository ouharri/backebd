package com.ouharri;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Integration tests for the jobinow application.
 * These tests ensure that the application context loads successfully.
 */
@SpringBootTest
class JobinowTests {

    /**
     * Test to verify that the application context loads without errors.
     */
    @Test
    void contextLoads() {
        // The test is empty as it only checks if the context loads successfully.
    }

}
